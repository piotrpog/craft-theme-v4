{% if block.headerMain %}
    {% include '_components/section-header' with {
        headerMain: block.headerMain,
        class: class ?? null,
        headerTag: block.headerIsMain ? 'h1' : 'h2',
    } only %}
{% endif %}